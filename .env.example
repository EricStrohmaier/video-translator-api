# API Keys (Required)
GOOGLE_VISION_API_KEY=your_google_vision_api_key_here
OPENAI_API_KEY=your_openai_api_key_here

# Server Configuration
PORT=3000
NODE_ENV=development
CORS_ORIGIN=*

# Storage Configuration
STORAGE_TYPE=local  # Options: local | s3 | r2

# S3/R2 Configuration (if STORAGE_TYPE=s3 or r2)
S3_BUCKET=your-bucket-name
S3_REGION=us-east-1
S3_ACCESS_KEY_ID=your_access_key
S3_SECRET_ACCESS_KEY=your_secret_key
# S3_ENDPOINT=https://your-account-id.r2.cloudflarestorage.com  # For Cloudflare R2

# Database Configuration
DB_TYPE=memory  # Options: memory | postgres | sqlite

# PostgreSQL (if DB_TYPE=postgres)
DATABASE_URL=postgresql://user:password@localhost:5432/videotranslator
DB_SSL=false

# PostgreSQL with Docker Compose (optional - for docker-compose.yml)
DB_USER=videotranslator
DB_PASSWORD=changeme
DB_NAME=videotranslator

# SQLite (if DB_TYPE=sqlite)
SQLITE_PATH=./jobs.db

# Subtitle Defaults (Optional - can be overridden via API)
ASS_BASE_FONTSIZE=44
ASS_BOX_PAD=10
ASS_MARGIN_V=90
SUB_TEXT_COLOR=#FFFFFF
SUB_BG_COLOR=#00000080
ASS_FORCE_ONE_LINE=0
ASS_FONT_URL=https://raw.githubusercontent.com/notofonts/noto-cjk/main/Sans/OTF/SimplifiedChinese/NotoSansCJKsc-Regular.otf
ASS_FONT_NAME=Noto Sans CJK SC
CJK_WIDTH_FACTOR=0.9
LATIN_WIDTH_FACTOR=0.62

# OCR/Text Detection Quality
MIN_TEXT_LENGTH=2  # Minimum character length for detected text
MIN_BOX_SIZE=5     # Minimum width/height in pixels for text boxes
FILTER_SYMBOLS=1   # Filter out decorative symbols (arrows, bullets, etc)
FILTER_NON_LATIN_SCRIPTS=1  # Filter out Arabic, Hebrew OCR artifacts

# Debug
DEBUG_COMPOSER=0
DEBUG_TRANSLATION=0
